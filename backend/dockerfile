# Brug en officiel Python runtime
FROM python:3.12.5-slim


# Arbejdsmappe i containeren
WORKDIR /app

# Kopiér requirements først (så Docker kan cache dem)
COPY requirements.txt .

# Installer dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Kopiér resten af backend projektet
COPY . .

# SSL certifikater vil blive monteret via volumes i docker-compose

# Flask/gunicorn skal lytte på port 5000
EXPOSE 5000

# Start serveren med Flask
CMD ["python", "app.py"]
